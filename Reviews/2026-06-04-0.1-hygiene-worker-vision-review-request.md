# AnvilRunner 0.1 Hygiene And Managed Worker Vision Review Request

## Intent

Review the follow-up release-hygiene and product-direction changes for AnvilRunner after the 0.1 readiness
merge.

The goal is to avoid scattering SwiftAnvil worker work across too many user-visible repositories while still
creating clean internal boundaries for runner lifecycle, capability discovery, host readiness, provisioning, and
fleet management.

## Current Branch

- Repository: `swiftanvil/swiftanvil-anvil-runner`
- Branch: `release/0.1-hygiene-worker-vision`
- Builder agent: Codex
- Review date: 2026-06-04

## Changes To Review

- Add release hygiene:
  - `CHANGELOG.md`
  - `SECURITY.md`
  - `.github/CODEOWNERS`
  - `.github/dependabot.yml`
  - local `Scripts/enforce-local.sh` shim
- Expand CI:
  - matrix over `macos-14` and `macos-15`
  - CLI smoke test with `swift run anvil-runner help`
- Update package documentation:
  - dependency snippet now targets `0.1.0`
  - local enforcement command documented
  - managed-worker direction documented
- Update package roadmap:
  - release hygiene is part of 0.1
  - read-only capability discovery and worker doctor come before provisioning
- Add `Documentation/MANAGED_WORKER_VISION.md`:
  - defines Worker, Runner, Host, Capabilities, Fleet
  - keeps one user-facing command surface
  - defers new public repositories until boundaries have a second concrete consumer
  - sequences capability discovery, doctor, provisioning, profiles, and fleet management

## Verification Already Run

- `swift test`
- `swift build`
- `Scripts/enforce-local.sh`
- workflow and Dependabot YAML parse
- source/doc line-length scan
- stale token/version/path scan

## Review Questions

1. Does this keep the product cohesive for users while allowing internal separation later?
2. Are the 0.1 release-hygiene additions enough before tagging `0.1.0`?
3. Is the managed-worker phase order correct: capability discovery, doctor, provisioning, profiles, fleet?
4. Should any of these changes be split out before PR creation?
5. Are there any missing organization-standard practices that should block this PR?

## Required Output

Return:

- `VERDICT: APPROVED`, `VERDICT: APPROVED_WITH_NOTES`, or `VERDICT: NEEDS_REVISION`
- blocker findings first
- non-blocking recommendations
- a concise release-readiness table
